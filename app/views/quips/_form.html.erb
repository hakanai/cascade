<%#
 % Partial for quip form.
 %
 % locals:
 %    f      - the form builder (object should be a Quip)
-%>

  <%= f.error_messages %>

  <p>
    <%= f.label :text, 'Markdown text' %><br/>
    <%= f.text_area :text %>
  </p>

  <div class="form-actions">
    <p> <%= f.submit(f.object.new_record? ? 'Create Quip' : 'Update Quip') %></p>
  </div>

